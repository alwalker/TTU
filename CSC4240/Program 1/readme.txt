To run the "Eliza" program type clisp -i eliza.lsp from your terminal.
To talk to Eliza use the following command:
	(eliza '("Say something here))
Without the quotes.
To exit type (exit).

Eliza will not work properly if anything you say to here contains a ' or the word QUOTE.
